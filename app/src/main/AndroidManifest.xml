<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.texephyr.texephyr2k16">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutScreen" />
        <activity android:name=".EventsScreen" />
        <activity android:name=".ComputerEventsScreen" />
        <activity android:name=".MechanicalEventsScreen" />
        <activity android:name=".ItEventsScreen" />
        <activity android:name=".EntcEventsScreen" />
        <activity android:name=".ContactScreen"></activity>
    </application>

</manifest>